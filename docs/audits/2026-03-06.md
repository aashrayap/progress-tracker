# Audit 2026-03-06

## Summary
- 12 findings across 4 layers
- HIGH: 3 | MEDIUM: 6 | LOW: 3

## L0: Core Docs
| Finding | Confidence | Detail |
|---------|-----------|--------|
| features.md claims `/reflect` redirects to `/mind` | HIGH | `/reflect` route was deleted this session. Remove line 15. |
| features.md claims `/quotes` redirects to `/resources` | MEDIUM | `/quotes/page.tsx` does redirect to `/resources`, but both are low-gravity. If killing these pages, update features.md. |
| features.md lists `/ideas` and `/resources` as active surfaces | MEDIUM | Both flagged for removal in prior hub-restructure decision. Still live. |
| data-schemas.md documents `quotes.csv` and `resources.csv` | LOW | Accurate schemas, but these CSVs only serve low-gravity pages. |
| MEMORY.md: "Reflect" still listed in nav description | LOW | Nav section says "Hub \| Plan \| Health \| Mind \| Reflect \| Ideas". Actual nav: Vision \| Hub \| Plan \| Health \| Mind \| Ideas \| Resources. |
| checkin-plan-integration.md in docs/specs/ | LOW | Unshipped spec. Verify if still planned or should be archived. |

## L1: Core Data
| Finding | Confidence | Detail |
|---------|-----------|--------|
| `quotes.csv` — only used by `/quotes` page + `/resources` page | MEDIUM | Dies if those surfaces are killed. |
| `resources.csv` — only used by `/resources` page | MEDIUM | Same. |
| `quotes.csv`, `resources.csv` not in CLAUDE.md canonical list | MEDIUM | Not listed as canonical but exist in data/. Either add or remove. |

## L2: Intelligence
| Finding | Confidence | Detail |
|---------|-----------|--------|
| `lib/vision.ts` — only imported by `api/vision/route.ts` | HIGH | Vision page never calls `/api/vision`. Both are dead code. |
| `api/vision/route.ts` — zero page consumers | HIGH | No `.tsx` file fetches `/api/vision`. |
| `api/daily-signals/route.ts` — 1 consumer (health/page.tsx) | LOW | Still used, but Hub moved to `/api/hub`. Not dead, just low-gravity. |

## L3: Surfaces
| Finding | Confidence | Detail |
|---------|-----------|--------|
| BottomNav has 7 tabs | MEDIUM | Vision, Hub, Plan, Health, Mind, Ideas, Resources. Prior decision recommended cutting to 4-5. |

## Recommended Actions
1. Delete `lib/vision.ts` + `api/vision/route.ts` (dead code, vision page uses hardcoded data)
2. Decide: kill `/ideas`, `/resources`, `/quotes` surfaces (prior decision pending)
3. Update features.md line 15 (remove `/reflect` redirect claim)
4. Update MEMORY.md nav description to match actual BottomNav
5. Decide: archive or execute `checkin-plan-integration.md` spec
