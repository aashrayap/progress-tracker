# Audit 2026-03-20

## Summary
- 16 findings across 4 layers
- HIGH: 5 | MEDIUM: 7 | LOW: 4
- Prior audit (2026-03-09): 3 resolved (ReflectPanel deleted, /quotes page deleted, shipped specs archived), 4 recurring

## L0: Core Docs
| Finding | Confidence | Detail |
|---------|-----------|--------|
| `experiments.csv` missing from CLAUDE.md CSV headers | MEDIUM | CSV exists, used by `csv.ts`, `api/hub`, `ExperimentsTable`, `vision/page`. In `data-schemas.md` but absent from CLAUDE.md quick ref. |
| `briefing_feedback.csv` still not in CLAUDE.md quick ref | MEDIUM | Recurring from 2026-03-09. In `data-schemas.md` but absent from CLAUDE.md + architecture.md. |
| `life-playbook.md` — zero code references | LOW | Recurring. Only in CLAUDE.md "Read Before Editing" table. Fold into app-intent? |
| MEMORY.md stale: claims `/api/gym`, `/api/work` exist | HIGH | These routes were removed. Also claims GYM_ROTATION in config.ts (it's in csv.ts). Route list outdated. |

## L1: Core Data
| Finding | Confidence | Detail |
|---------|-----------|--------|
| `data/relationship.md` — not a CSV, no app consumers | MEDIUM | Skill-only data. Consider `.claude/skills/relationship/` instead of `data/`. |
| `quotes.csv` + `api/quotes/route.ts` — page deleted | MEDIUM | Recurring. `/quotes` page gone but API route + CSV remain. Used by `/resources` page fetch. |

## L2: Intelligence
| Finding | Confidence | Detail |
|---------|-----------|--------|
| `api/reflections/route.ts` — zero page consumers | HIGH | No `.tsx` calls `/api/reflections`. Skills-only endpoint. |
| `api/mind/route.ts` — only consumer is orphaned `/mind` page | HIGH | `/mind` not in nav. Both page + API orphaned together. |
| Unused exports in `csv.ts`: `appendExperiment`, `updateExperiment` | MEDIUM | Exported but never called. Experiments write path is dead. |
| Unused exports in `csv.ts`: `BriefingFeedbackEntry`, `MetricHistoryEntry`, `TodoEntry` | MEDIUM | Exported interfaces, zero consumers. |
| Unused types in `types.ts` | MEDIUM | `GroupedExercise`, `EatingSummary`, `LanguageRules`, `IdentityScript`, `RitualBlock`, `TriggerReplacement` — never imported. |
| `lib/insight.ts` — single consumer | LOW | Only `api/hub/route.ts`. Recurring awareness item. |

## L3: Surfaces
| Finding | Confidence | Detail |
|---------|-----------|--------|
| `/mind/page.tsx` — not in nav, not linked | HIGH | Orphaned surface. No path to reach it except direct URL. |
| `BottomNav.tsx` named wrong — renders as top nav | LOW | File says BottomNav, exports TopNav, renders `sticky top-0`. Misleading filename. |
| All 24 components actively imported | ✓ | No orphaned components (ReflectPanel resolved from prior audit). |
| Layer boundary integrity clean | ✓ | No CSV reads in components, all data via API routes. |

## Recommended Actions
1. Delete `/mind/page.tsx` + `api/mind/route.ts` (orphaned pair, zero nav links)
2. Update MEMORY.md: remove `/api/gym`, `/api/work`, fix GYM_ROTATION location, update route list
3. Add `experiments.csv` + `briefing_feedback.csv` to CLAUDE.md CSV quick ref
4. Clean unused exports: `appendExperiment`, `updateExperiment`, dead types in `types.ts`
5. Decide: keep or delete `api/quotes/route.ts` + `quotes.csv`
6. Decide: keep `api/reflections/route.ts` (skill-only) or remove
7. Move `data/relationship.md` to `.claude/skills/relationship/`
8. Rename `BottomNav.tsx` → `TopNav.tsx` (already exports TopNav)
