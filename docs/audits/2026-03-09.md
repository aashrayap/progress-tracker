# Audit 2026-03-09

## Summary
- 11 findings across 4 layers
- HIGH: 3 | MEDIUM: 5 | LOW: 3
- Prior audit (2026-03-06): 2 findings resolved, 3 recurring

## L0: Core Docs
| Finding | Confidence | Detail |
|---------|-----------|--------|
| `briefing_feedback.csv` missing from `data-schemas.md` | MEDIUM | CSV exists in `data/`, referenced by `BriefingCard.tsx` and `api/hub/briefing-feedback/route.ts`. No schema documented. |
| `api/hub/briefing-feedback` missing from architecture.md API list | MEDIUM | Route exists but not in "API Surface (Current)" section. |
| `docs/specs/` has 3 draft specs + 2 shipped specs | LOW | Shipped: `checkin-plan-integration`, `mind-rework`. Drafts: `context-aware-briefing-card`, `deterministic-voice-pipeline`, `context-aware-briefing-card-interview`. Consider archiving shipped specs. |
| `life-playbook.md` — zero references in app code | LOW | Only referenced by CLAUDE.md "Read Before Editing" table. Prior conversation flagged for folding into app-intent.md. |
| `docs/artifacts/` — 3 untracked directories with research/questions | LOW | `hub-top-card/`, `structured-data-layer/`, `time-aware-planning/`. Feature-interview artifacts. Review if still active. |

## L1: Core Data
| Finding | Confidence | Detail |
|---------|-----------|--------|
| `code-todos.csv` — zero app consumers | HIGH | Documented in `data-schemas.md` and CLAUDE.md CSV quick ref, but no `.ts`/`.tsx` file reads it. Only surfaced via `/checkin` skill (prompt-level, not code). |
| `quotes.csv`, `resources.csv` not in CLAUDE.md canonical data list | MEDIUM | Recurring from 2026-03-06. Listed in `data-schemas.md` but absent from CLAUDE.md "Canonical Data Layer" in architecture.md. |
| `briefing_feedback.csv` not documented anywhere | MEDIUM | No schema in `data-schemas.md`, no mention in CLAUDE.md. |

## L2: Intelligence
| Finding | Confidence | Detail |
|---------|-----------|--------|
| `lib/insight.ts` — single consumer (`api/hub/route.ts`) | LOW | Not dead, but narrow usage. Awareness only. |

## L3: Surfaces
| Finding | Confidence | Detail |
|---------|-----------|--------|
| `ReflectPanel.tsx` — zero imports | HIGH | No page or component imports this. Dead component. |
| `/quotes/page.tsx` — redirect-only page | MEDIUM | Redirects to `/resources`. Could be deleted if no external links depend on it. |

## Recommended Actions
1. Delete `ReflectPanel.tsx` (dead component, zero imports)
2. Add `briefing_feedback.csv` schema to `data-schemas.md` + add route to architecture.md API list
3. Decide: keep or delete `/quotes/page.tsx` redirect
4. Add `quotes.csv`, `resources.csv`, `briefing_feedback.csv` to architecture.md canonical data list (or decide to remove the CSVs)
5. Archive shipped specs (`checkin-plan-integration`, `mind-rework`) or move to `docs/specs/archived/`
6. Decide: fold `life-playbook.md` into `app-intent.md` (flagged in prior conversation, still open)
